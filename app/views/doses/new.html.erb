<h1> Ajoute tes doses pour <%= @cocktail %></h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, as: :select  %>
  <%= f.submit "Ajout Dose", class: "btn btn-primary" %>
  <% end %>
